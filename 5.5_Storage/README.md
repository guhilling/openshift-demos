# Storage Examples

Using Storage with examples for shared storage: __Deployment__ and a __ReadWriteMany__ volume. 

And a __StatefulSet__ combined with a __ReadWriteOncePod__ volume to make sure that at any time at most 1 instance of the application (MariaDB) is running.

