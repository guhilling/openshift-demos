# Pod Configuration

This example shows how to externalize configuration using __Secrets__ and __ConfigMaps__.

The application uses environment variables as well as volume mappings.

The index.html file is provided only for editing purposes.
